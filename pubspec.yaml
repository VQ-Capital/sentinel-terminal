# ========== DOSYA: sentinel-terminal/pubspec.yaml ==========
name: sentinel_terminal
description: "VQ-Capital Sentinel Dashboard"
publish_to: 'none'
version: 1.0.2+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  protobuf: ^6.0.0
  web_socket_channel: ^3.0.3
  riverpod: ^2.4.0
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  stream_transform: ^2.1.1
  http: ^1.2.0 # 🔥 CERRAHİ: REST API (Diagnose) Okumak İçin Eklendi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true